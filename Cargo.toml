[package]
name = "rwasm-fuel-policy"
version = "0.0.1"
edition = "2021"
description = "Fuel policies for rWasm VM"
license = "Apache-2.0"

[dependencies]
wasmparser = { version = "0.243.0", default-features = false, features = ["simd"] }

[features]
default = ["std"]
std = ["wasmparser/std"]